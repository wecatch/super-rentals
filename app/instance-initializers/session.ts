import ApplicationInstance from '@ember/application/instance';

export function initialize(owner: ApplicationInstance) {
  // owner.inject('route:session', 'session', 'service:session');
}

export default {
  initialize,
};
